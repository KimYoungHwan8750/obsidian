```properties
spring.profiles.active=server
# 값을 바인딩 할 때 쌍따옴표를 적지 않는다.
# 해당 설정을 사용하면 resources에 있는 application-server.properties를 참조한다.
# application.properties를 먼저 참조한 후 custom properties를 참조한다. 중복된 값이 있으면 나중에 작성된 값으로 덮어씌워진다.
```


