현재 위치의 파일과 폴더 목록 표시

## Option
`-a`: 숨김 파일도 표시
`-l`: 파일 상세 정보도 함께 표시
`-R`: 하위 디렉터리 내용까지 모두 표시
`-h`: (human-readable):** 파일 크기를 K, M, G 단위로 읽기 쉽게 표시
`-S`: 파일 크기가 큰 순서대로 정렬.
`-d`: 디렉토리 자체의 권한을 보여줌 (`ls /a` 명령어는 a폴더의 구조를 보여주지만 `ls -d /a`는 a라는 폴더 자체의 정보를 표시함)

**예제**
`ls -al`: 숨김 파일과 상세 정보 함께 표시