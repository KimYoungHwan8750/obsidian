```java
public CommonMap getPrdtDetailList(HttpServletRequest request) {
	CommonMap result = new CommonMap();
	CommonMap param = MapUtils.parseRequest(request);
	try {
		param.put("queryId", "prdt.prdtManage.select_detailList");
		List<CommonMap> detailList = commonService.select(param);
```

count 필드에
```java
public final class CommonConstants {

	// 생성자 private으로 정의하여 인스턴스화 방지
    private CommonConstants() {}
    
    public static final int INIT_RESULT = 0; // 초기값 세팅
	public static final int PROC_ERROR = 0; // 정상적으로 처리되지 않음
	public static final int PROC_SUCCESS = 1; // 정상 처리
	public static final int VALUE_ERROR = -2000; // 필수값을 못받았을때 반환시켜줄 코드
	public static final int INFO_EMPTY = -2100; // 조회된 정보가 없을 때
	public static final int DUPLICATE_ERROR = -3000; // 중복된 값이 존재할 때
}

```

위와 같은 값을 반환함으로써 성공/실패를 핸들링한다. count>0일 경우 성공이라고 보면 된다.
쿼리 결과는 CommonMap에 camelCase로 들어온다.