```js
parameters: {
    docs: {
      source: {
        code: `<Button>Button</Button>`, // ← 여기서 보여줄 코드 직접 지정
      },
    },
  },

```

```js
  parameters: {
    controls: {
      exclude: ["icon", "iconPosition", "fullWidth"],
    },
  }
```