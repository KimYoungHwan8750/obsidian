

```text
# 유니코드 경로를 그대로 표시
git config --global core.quotepath false
# Git 메시지에서 한글 깨짐 방지
git config --global core.precomposeunicode true

# Git 기본 인코딩을 UTF-8로 설정
git config --global core.encoding utf-8
```

```dataview
TABLE without id
김영환
FROM "무제 파일"
```


