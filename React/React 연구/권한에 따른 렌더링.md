권한에 따른 렌더링을 제어할 때 주의해야할 점이 있다.
권한을 확인하기 전엔 요소가 렌더링되지 않고, 쿼리를 날려 데이터를 받아오고 난 후에야 렌더링이 결정된다.
따라서 컴포넌트 전체를 권한에 따라 처리할 것이 아니라, 최소 크기를 가진 wrapper 컴포넌트를 두고 그 child를 렌더링해야한다.

```tsx
function AuthButton() {
	return (
		<div>
			{isAuth <Button></Button>
		</div>
	)
}

```