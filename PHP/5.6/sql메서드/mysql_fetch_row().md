쿼리 결과에서 데이터 한 줄 가져온다.
이터레이터를 생각하면 유사하지만 PHP는 포인터 개념이 있으므로 사실 내부적으로 포인터를 이동시켜서 참조를 바꾸는 형식으로 구현된다.

```php
$rs = mysql_fetch_row();
while($row = mysql_fetch_row($rs)) {
	echo "ID: " . $row[0] . "PW: " . $row[1];
}
```

또한 특이한 점으로는 컬럼의 이름을 키값으로 쓰지 않고 순서를 키값으로 쓰기 때문에 컬럼 순서대에 맞는 인덱스로 접근해야한다.